cmake_minimum_required(VERSION 3.25)
project(List)
include_directories(include)
file(GLOB SOURCES "src/*.cpp")
add_executable(List ${SOURCES})
file(GLOB TXT_FILES "src/*.txt")
file(COPY ${TXT_FILES} DESTINATION ${CMAKE_BINARY_DIR}/Release)
